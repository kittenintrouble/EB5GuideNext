import XCTest

final class EB5GuideNextUITests: XCTestCase {
    func testEmpty() {
        // UI tests removed for now. Delete the target from the project if not needed.
        XCTAssertTrue(true)
    }
}
